# 🚀 Quick Start Guide - LineOA v2

## ติดตั้งและเริ่มใช้งานภายใน 5 นาที!

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
# 1. ติดตั้ง Backend (ต้องรันในโฟลเดอร์ lineoav2)
npm install

# 2. ติดตั้ง Frontend
cd client
npm install
cd ..
```

### ขั้นตอนที่ 2: รัน Application

**วิธีที่ 1: รันคำสั่งเดียว (แนะนำ)**
```bash
npm run dev
```
*Backend (port 5000) และ Frontend (port 3000) จะทำงานพร้อมกันอัตโนมัติ*

---

**วิธีที่ 2: รันแยก 2 Terminal (สำหรับ Windows หรือถ้า concurrently ไม่ทำงาน)**

**Terminal 1 - Backend:**
```bash
npm run server
```

**Terminal 2 - Frontend:**
```bash
npm run client
```

### ขั้นตอนที่ 3: เข้าใช้งาน

1. เปิดเบราว์เซอร์ไปที่: **http://localhost:3000**
2. Login ด้วยบัญชี Default หรือสร้างบัญชีใหม่

**บัญชี Default:**
- Username: `Test01`
- Password: `123456789`

---

## ✨ ฟีเจอร์หลัก

### 📝 ลงทะเบียนบัญชีใหม่
1. คลิก "ลงทะเบียน" ที่หน้า Login
2. กรอก Username (อย่างน้อย 3 ตัวอักษร)
3. กรอก Password (อย่างน้อย 6 ตัวอักษร)
4. ยืนยัน Password
5. คลิก "ลงทะเบียน"

### 🖼️ ส่งรูปภาพ
1. คลิกปุ่ม **📎** ในช่องพิมพ์ข้อความ
2. เลือกรูปจากเครื่อง (สูงสุด 10MB)
3. ดู Preview แล้วคลิก "ส่ง"

### 😊 ส่งสติกเกอร์
1. คลิกปุ่ม **😊** ในช่องพิมพ์ข้อความ
2. เลือกสติกเกอร์จาก Grid
3. สติกเกอร์จะถูกส่งทันที

### 💬 แชทกับลูกค้า LINE
1. เพิ่ม LINE Channel (Channel Secret & Access Token)
2. ตั้งค่า Webhook: `http://your-domain/webhook/:channelId`
3. เลือกการสนทนาจากรายการด้านซ้าย
4. พิมพ์ข้อความ หรือส่งรูป/สติกเกอร์

---

## 📁 โครงสร้างข้อมูล

ข้อมูลทั้งหมดถูกเก็บในโฟลเดอร์ `/data`:

- `users.json` - ข้อมูลผู้ใช้งาน
- `channels.json` - ข้อมูล LINE Channels
- `messages.json` - ประวัติข้อความ
- `groups.json` - กลุ่มการสนทนา

รูปภาพทั้งหมดถูกเก็บในโฟลเดอร์ `/uploads`

---

## 🔧 การแก้ไขปัญหา

### ❌ 'concurrently' is not recognized as an internal or external command

**สาเหตุ:** ยังไม่ได้ติดตั้ง dependencies

**วิธีแก้:**
```bash
# ติดตั้ง dependencies ใหม่
npm install
```

ถ้ายังไม่ได้ผล ให้รันแยก 2 Terminal แทน:
```bash
# Terminal 1
npm run server

# Terminal 2  
npm run client
```

### Server ไม่สามารถรันได้
```bash
# ลบ node_modules และติดตั้งใหม่
rm -rf node_modules
npm install
```

### Frontend ไม่สามารถรันได้
```bash
cd client
rm -rf node_modules
npm install
cd ..
```

### ไม่สามารถอัปโหลดรูปได้
ตรวจสอบว่าโฟลเดอร์ `uploads` มีสิทธิ์เขียนไฟล์:
```bash
mkdir -p uploads
chmod 755 uploads
```

---

## 📞 ติดต่อและสนับสนุน

หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อ:
- Email: support@example.com
- LINE: @yourlineid

---

**Happy Chatting! 🎉**
